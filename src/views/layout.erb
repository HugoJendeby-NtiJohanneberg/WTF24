<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>

   <header>
        <nav>
            <a href="/">Home</a>
            <a href="/movies">Movies</a>
            <a href="/movies/new">New Movie</a>
            <% if logged_in? %>
                <span>Welcome, <%= h(current_user['username']) %>!</span>
                <%= logout_button %>
            <% else %>
                <a href="/login">Login</a>
                <a href="/register_user">Register</a>
            <% end %>
        </nav>
    </header>

    <%= yield %>

</body>
</html>